
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Primeiros Passos</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', sans-serif;
      background: #121212;
      color: #f5f5f5;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }

    .container {
      background: #1e1e1e;
      padding: 2rem;
      border-radius: 20px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.4);
      max-width: 500px;
      width: 100%;
    }

    .container h2 {
      text-align: center;
      margin-bottom: 1.5rem;
      color: #ffffff;
    }

    .profile-pic {
      position: relative;
      width: 120px;
      height: 120px;
      margin: 0 auto 1.5rem;
      cursor: pointer;
      border-radius: 50%;
      background-color: #2c2c2c;
      background-size: cover;
      background-position: center;
      overflow: hidden;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .profile-pic input {
      display: none;
    }

    .placeholder-text {
      color: #aaa;
      font-size: 0.85rem;
      pointer-events: none;
      text-align: center;
      z-index: 1;
    }

    .hidden {
      display: none;
    }

    .input-group {
      margin-bottom: 1.2rem;
    }

    .input-group label {
      display: block;
      font-weight: 600;
      margin-bottom: 0.4rem;
      color: #ccc;
    }

    .input-group input,
    .input-group textarea {
      width: 100%;
      padding: 0.7rem;
      background-color: #2c2c2c;
      border: 1px solid #444;
      border-radius: 10px;
      font-size: 0.95rem;
      color: #f5f5f5;
    }

    .input-group input:focus,
    .input-group textarea:focus {
      outline: none;
      border-color: #6366f1;
    }

    .btn {
      width: 100%;
      padding: 0.9rem;
      background-color: #4f46e5;
      color: white;
      border: none;
      border-radius: 10px;
      font-weight: bold;
      cursor: pointer;
      transition: background 0.3s ease;
    }

    .btn:hover {
      background-color: #3730a3;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>Primeiros Passos</h2>

    <div class="profile-pic" id="profilePic" onclick="document.getElementById('inputFoto').click()">
      <div id="placeholder" class="placeholder-text">Foto de Perfil</div>
    </div>
    <input type="file" accept="image/*" id="inputFoto" style="display: none;">

    <div class="input-group">
      <label for="nome">Nome</label>
      <input type="text" id="nome" placeholder="Seu nome completo">
    </div>

    <div class="input-group">
      <label for="bio">Biografia</label>
      <textarea id="bio" rows="3" placeholder="Conte um pouco sobre você..."></textarea>
    </div>

    <div class="input-group">
      <label for="profissao">Profissão</label>
      <input type="text" id="profissao" placeholder="Sua área de atuação (ex: Desenvolvedor)">
    </div>

    <button class="btn" id="btnSalvar">Finalizar</button>
  </div>

  <script type="module" src="/js/primeirosPassos.js"></script>
</body>
